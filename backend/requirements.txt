# ————— API + SERVER ——————————————————————
groq
fastapi
uvicorn[standard]
websockets
python-multipart
httpx

# ————— ENV + CONFIG ——————————————————————
python-dotenv
pathlib2
psutil
requests

# ————— SECURITY + STORAGE ————————————————
cryptography
keyring
pywin32; sys_platform == 'win32'
winsdk; sys_platform == 'win32'
pycaw; sys_platform == 'win32'

# ————— PRIVACY / PII ─────────────────────
transformers>=4.40.0
sentencepiece                     # tokenizer for deberta-v3
protobuf

# ————— AUDIO + VOICE —————————————————————
pyaudiowpatch; sys_platform == 'win32'
sounddevice
numpy
resemblyzer
torch
torchaudio
pydub

# ————— SCREEN + VISION ———————————————————
# xdotool required on Linux (apt install xdotool)
uiautomation; sys_platform == 'win32'
# macOS counterpart to uiautomation: ApplicationServices provides the
# Accessibility (AX) element walk, Quartz the window-region capture, Cocoa the
# frontmost-app lookup. Requires Accessibility + Screen Recording permission.
pyobjc-framework-Cocoa; sys_platform == 'darwin'
pyobjc-framework-ApplicationServices; sys_platform == 'darwin'
pyobjc-framework-Quartz; sys_platform == 'darwin'
Pillow
mss
opencv-python
pygetwindow; sys_platform == 'win32'
ultralytics
easyocr

# ————— MEMORY + DATA —————————————————————
scipy
scikit-learn

# ————— CALENDAR SKILL ————————————————————
icalendar
recurring-ical-events

# ————— SKILLS ————————————————————————————
pypdf
reportlab
pdf2image
python-docx
docx2pdf; sys_platform == 'win32'
openpyxl
pandas
xlrd
python-pptx
pyzbar
piexif
ffmpeg-python
SpeechRecognition
tabulate
rarfile
py7zr
pygments
black
isort
comtypes; sys_platform == 'win32'
edge-tts
pygame                            # audio playback in voice.py (imported at module level)
pyttsx3
pyperclip
keyboard
pyautogui
duckduckgo-search
rapidfuzz
pyyaml                            # SKILL.md frontmatter parsing (adapted_skill.py)
markitdown                        # how the official pptx/xlsx skills read existing files

# ————— WEB REACH (fetch_webpage / fetch_youtube_transcript / fetch_rss_feed) —
youtube-transcript-api
feedparser

# ————— BACKUP ────────────────────────────────────────────────────────────────
mnemonic                          # BIP-39 wordlist + checksum utilities
boto3                             # S3 / Backblaze B2 / Cloudflare R2 / Wasabi
google-api-python-client          # Google Drive
google-auth-oauthlib              # Google OAuth flow
dropbox                           # Dropbox SDK
