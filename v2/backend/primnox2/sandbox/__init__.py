"""Sandbox Manager — a kernel service, not a utility under the tool service.

Tools do not execute code. They request execution, and this package decides
whether it happens, under what limits, and what is left behind afterwards.
"""
