/* Phase 1 stub — PermissionBlock is a Phase 3+ feature (tools / assets / canvas / privacy).
   TurnBlock maps over empty arrays for these, so this never renders. Ported
   verbatim from frontend/ when its phase lands. */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function PermissionBlock(_props: any): null {
  return null;
}
