/* Trimmed primitive set for the web build — just what the ported transcript
   needs. The desktop set (Button/Field/Chip/Slider/Section) lands when those
   surfaces are ported. */
export { Panel } from './Panel';
export { Skeleton, RowSkeleton, ListSkeleton } from './Skeleton';
